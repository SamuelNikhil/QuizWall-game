# ============================================================
# Game Server Configuration
# IMPORTANT: Include the port here for the Vite proxy to work
# ============================================================

# Your game server URL (used in DIRECT mode)
VITE_SERVER_URL=http://10.142.139.11
VITE_SERVER_PORT=3000

# ============================================================
# Connection Mode
# ============================================================
# Values:
#   - 'true' / 'proxy' / '1': PROXY mode (connect through hosting provider)
#   - 'false' / 'direct':      DIRECT mode (connect to server URL directly)
#
# IMPORTANT: 
#   - PROXY mode only works if your hosting provider supports WebSocket proxying
#   - Vite dev/preview server supports proxy (configure in vite.config.ts)
#   - Netlify supports proxy via _redirects
#   - Render.com supports proxy via _redirects rewrite rules (status 200)
#   - For Render: set VITE_USE_PROXY=true and add /.wrtc/* proxy rule in _redirects
VITE_USE_PROXY=false

# Signaling Path for Geckos.io (used in both proxy and direct modes)
# Geckos.io v3 uses /.wrtc/v2 by default
VITE_SIGNALING_PATH=/.wrtc/v2